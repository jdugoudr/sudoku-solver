#ifndef H_RESOLVER_H
	#define H_RESOLVER_H
	#include "header.h"
	#include "check.h"

	int	ft_possibilty(s_map map[81]);
	int	ft_putIfOne(s_map map[81]);
	int	ft_resolve(s_map map[81]);
	int ft_less(s_map map[81]);
	int ft_end(s_map map[81]);

#endif
